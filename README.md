# dotnet-test-mcp

Minimal scaffold for a console-hosted MCP server using the Model Context Protocol C# SDK.

## Run

```bash
dotnet run --project src/DotnetTest.Mcp
```

## Tools

Tools are discovered from the server assembly. The scaffold ships with `EchoTool` as a simple example.

## Test

```bash
dotnet test
```
